# 36. Architecture Implementation Ledger

Implementation of [the audit roadmap](./35_full_stack_architecture_audit.md), started 2026-09-06 on local `dev`. No deployment, production migration, package extraction, or automatic commits are part of this execution.

| Item | Status   | Evidence / remaining work                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R00  | Complete | 79 tests across 19 contract suites discovered; three negative/positive discovery tests pass. Contract baseline reconciled against current runtime. Isolated PostgreSQL fixtures pass.                                                                                                                                                                                                                                                                    |
| R01  | Complete | Private list, lots, summary, access, and settings routes use verified sessions only; forged guest-cookie tests pass. Legacy guest data remains untouched, access disabled.                                                                                                                                                                                                                                                                               |
| R02  | Complete | `pnpm test:db` passes 10 isolated PostgreSQL tests, including actual adapter success and later-write rollback. Additive migration 0009 repairs missing series/transaction schema; legacy IDs untouched.                                                                                                                                                                                                                                                  |
| R03  | Complete | Typed lot patches reconcile dependent type/series IDs, reject unavailable explicit series, preserve omitted fields, and retain source/destination owner predicates. 43 focused route/schema/command tests pass. The isolated real-session browser journey now covers create, transactional lot save, edit, and reload; it awaits an external DB run.                                                                                                     |
| R04  | Complete | Shared page now consumes allowlisted serializable public metadata only (`name`, `description`); no owner controller, fetches, or mutation callback. Lots remain undisclosed pending product decision. The isolated browser journey asserts public metadata, no private portfolio requests, no controls, and CSP; it awaits an external DB run.                                                                                                           |
| R05  | Active   | Failure containment is complete: 6 handler tests pass, including identity and limiter rejection. The PostgreSQL limiter adapter now has isolated concurrency/reset test coverage under `pnpm test:db`; it awaits an isolated DB run. Trusted ingress header semantics remain deployment work.                                                                                                                                                            |
| R06  | Complete | Theme storage/subscription, safe access error state, obsolete series cancellation, and document locale update are covered by focused tests plus locale parity. The normal browser suite now has a theme/locale preference proof; browser execution remains release validation.                                                                                                                                                                           |
| R07  | Complete | Resolved runtime reachability analysis, 80 contract tests, 13 architecture tests, explicit discovery guard, CI discovery/unused gates all pass; unused scan is clean.                                                                                                                                                                                                                                                                                    |
| R08  | Complete | Envelope/raw codecs normalize malformed JSON, problems, and correlation. Worker failure transport preserves reviewed public metadata, hides raw transport errors. 21 focused tests pass.                                                                                                                                                                                                                                                                 |
| R09  | Complete | Production composition moved to `lib/server/calculation`; handlers receive data adapters; generic JSON reader no longer imports calculation domain. Focused application, dependency, validation, and golden tests pass.                                                                                                                                                                                                                                  |
| R10  | Active   | Five-minute cache now has explicit independent-instance semantics; calculation cache revision includes exact definitions read. Cache/context tests pass. Durable sync-revision policy remains product/infrastructure decision.                                                                                                                                                                                                                           |
| R11  | Complete | Workspace access/portfolio GETs use identity-scoped SWR resources. Notebook controller owns access capability and exposes prepared `view` plus named `actions`; raw feedback setters stay internal. Subscriber, selection restoration, disabled, and failure tests pass.                                                                                                                                                                                 |
| R12  | Complete | Existing `useBondCalculator` and `useComparison` already own feature lifecycle/session seams. Added obsolete-series regression proof; no extra generic controller would hide meaningful behavior or improve locality.                                                                                                                                                                                                                                    |
| R13  | Complete | Telemetry route labels remove identifiers/query strings; transport, aggregate, payload, and logging privacy tests pass (17 tests).                                                                                                                                                                                                                                                                                                                       |
| R14  | Complete | Rejected: no presentation slice demonstrated independent behavior. No line-count or visual-taxonomy extraction made.                                                                                                                                                                                                                                                                                                                                     |
| R15  | Active   | Real-auth browser config now builds/runs with smoke disabled, CSP enforced, an isolated DB session fixture, and deterministic cleanup. Typecheck, lint, formatting, contracts, architecture, focused behavior, unused scan, and isolated DB proofs pass. `pnpm build` is blocked by sandbox port binding during Turbopack CSS compilation; run `TEST_DATABASE_URL=<isolated database> pnpm test:browser:integration` and rerun build in normal local/CI. |

## Validation policy

Focused behavioral tests precede status completion. Broader type, lint, formatting, contracts, core, build, database and browser gates follow the relevant changes. Existing audit findings are historical; this ledger records current implementation evidence and external limitations.
